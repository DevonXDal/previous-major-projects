export class Constants {
    public static apiRoot = "http://localhost:80/api"

    public static apiVersion = "v1"

    public static noResponseFromApi = "The API did not respond, please watch to see if the status has changed"
}